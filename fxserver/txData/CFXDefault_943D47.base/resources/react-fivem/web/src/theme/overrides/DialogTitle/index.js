import palette from '../../palette';

const MuiDialogTitle = {
  root: {
    '&.modal-title': {
      background: 'linear-gradient(167deg, rgba(15,61,53,100) 0%, rgba(0,154,119,100) 100%)',
      color: palette.white
    }
  }
};

export default MuiDialogTitle;
